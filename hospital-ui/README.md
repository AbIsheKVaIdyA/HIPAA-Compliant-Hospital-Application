# Hospital UI (frontend)

This folder contains the React + Vite + Tailwind **hospital UI** for the HIPAA-Compliant Hospital Application.

For full project details (tech stack, dependency versions, setup steps, and roadmap), see the root project README in this repo.

Quick start from this folder:

```bash
npm install
npm run dev
```

